---
alwaysApply: true
---
# 🚫 Антипаттерны разработки: Как НЕ надо делать

> **Цель документа:** Собрать все ошибки и промахи из реальной разработки для предотвращения их повторения в будущем.

---

## 🔧 Технические ошибки

### ❌ Проблемы с именованием и структурой

**НЕ ДЕЛАТЬ:**
- Использовать неправильные имена переменных (`router` вместо `catalog_router`)
- Создавать дублирующие модели в разных файлах (`CatalogVersion` в `models.py` и `models_old.py`)
- Забывать про `extend_existing=True` в SQLAlchemy моделях при изменении схемы

**ПРАВИЛЬНО:**
- Всегда проверять точные имена переменных через `grep` перед использованием
- Удалять старые/дублирующие файлы сразу после рефакторинга
- Добавлять `extend_existing=True` при модификации существующих таблиц

### ❌ Проблемы с базой данных и миграциями

**НЕ ДЕЛАТЬ:**
- Создавать миграции с неправильными `down_revision` (копировать имена файлов вместо хешей)
- Добавлять поля в модели без соответствующих миграций
- Использовать разные поля для одной цели (`completed_at` vs `indexed_at`)

**ПРАВИЛЬНО:**
- Проверять существующие миграции и их хеши перед созданием новых
- Синхронизировать модели и миграции одновременно
- Использовать единую схему именования полей

### ❌ Проблемы с типами данных и парсингом

**НЕ ДЕЛАТЬ:**
- Предполагать что API всегда возвращает `datetime` объекты (может быть строка)
- Забывать про обработку часовых поясов при работе с `datetime`
- Использовать жестко заданные форматы времени по всему коду

**ПРАВИЛЬНО:**
- Всегда проверять тип данных перед обработкой (`isinstance(dt, str)`)
- Создавать централизованные утилиты для работы с временем
- Использовать конфигурационные файлы для форматов

---

## 🎯 Логические ошибки

### ❌ Неполная реализация функций

**НЕ ДЕЛАТЬ:**
- Добавлять поля в API ответ без реализации логики их заполнения
- Создавать функции с параметрами, которые не используются
- Забывать обновлять связанные части системы при изменениях

**ПРАВИЛЬНО:**
- Реализовывать функциональность полностью от начала до конца
- Тестировать все новые поля и параметры
- Проверять все места использования при изменениях

### ❌ Проблемы с условной логикой

**НЕ ДЕЛАТЬ:**
- Показывать "ожидаемое время завершения" для уже завершенных процессов
- Рассчитывать время для статусов, где это не имеет смысла
- Использовать одинаковую логику для разных статусов

**ПРАВИЛЬНО:**
- Четко разделять логику по статусам процессов
- Показывать релевантную информацию для каждого состояния
- Тестировать все ветки условий

---

## 🌐 Проблемы с веб-интерфейсом

### ❌ Ошибки в шаблонах

**НЕ ДЕЛАТЬ:**
- Использовать неправильный синтаксис Jinja2 (`{{ var }}` в JavaScript без кавычек)
- Забывать применять фильтры форматирования к полям времени
- Создавать алерты без возможности их закрытия

**ПРАВИЛЬНО:**
- Проверять синтаксис Jinja2 в JavaScript контексте
- Применять фильтры ко всем полям одного типа консистентно
- Всегда добавлять кнопки закрытия для уведомлений

### ❌ Проблемы с авторизацией и редиректами

**НЕ ДЕЛАТЬ:**
- Возвращать JSON ошибки для HTML запросов
- Забывать про глобальные обработчики исключений
- Смешивать логику авторизации в разных местах

**ПРАВИЛЬНО:**
- Использовать глобальные exception handlers для единообразия
- Различать HTML и API запросы для правильных ответов
- Централизовать логику авторизации

---

## 🔄 Проблемы с процессами и состояниями

### ❌ Неточное отслеживание времени

**НЕ ДЕЛАТЬ:**
- Записывать время завершения в неправильный момент
- Использовать приблизительное время вместо точного
- Забывать обновлять все связанные поля при завершении процесса

**ПРАВИЛЬНО:**
- Записывать время точно в момент фактического завершения
- Обновлять все связанные поля атомарно
- Тестировать временные метки в реальных сценариях

### ❌ Проблемы с управлением коллекциями

**НЕ ДЕЛАТЬ:**
- Предполагать что коллекция существует без проверки
- Выполнять операции с несуществующими коллекциями
- Игнорировать ошибки при работе с внешними системами

**ПРАВИЛЬНО:**
- Всегда проверять существование ресурсов перед операциями
- Обрабатывать случаи отсутствия ресурсов gracefully
- Логировать все операции с внешними системами

---

## 🧪 Проблемы с тестированием

### ❌ Неполное тестирование

**НЕ ДЕЛАТЬ:**
- Тестировать только "счастливый путь"
- Забывать тестировать граничные случаи
- Предполагать что исправления работают без проверки

**ПРАВИЛЬНО:**
- Тестировать все ветки кода и состояния
- Проверять работу с пустыми/null значениями
- Всегда верифицировать исправления в реальных условиях

### ❌ Проблемы с тестовыми данными

**НЕ ДЕЛАТЬ:**
- Полагаться на существующие данные в тестах
- Использовать хардкод в тестах без объяснения
- Забывать очищать тестовые файлы

**ПРАВИЛЬНО:**
- Создавать изолированные тестовые данные
- Документировать все предположения в тестах
- Автоматически очищать временные файлы

---

## 🐳 Проблемы с Docker и контейнеризацией

### ❌ Проблемы с монтированием файлов

**НЕ ДЕЛАТЬ:**
- Копировать `.env` файлы в Docker образ без монтирования как volume
- Предполагать что изменения в `.env` на хосте автоматически попадут в контейнер
- Забывать синхронизировать `.env` файлы между development и production окружениями

**ПРАВИЛЬНО:**
- Всегда монтировать `.env` файлы как volumes: `- ./.env:/app/.env`
- Проверять синхронизацию настроек между хостом и контейнером
- Использовать одинаковую схему монтирования в dev и prod

### ❌ Проблемы с кодировкой в терминалах

**НЕ ДЕЛАТЬ:**
- Использовать русские комментарии в `.env` файлах при работе в Windows PowerShell
- Игнорировать символы `????` в выводе терминала
- Смешивать кодировки в конфигурационных файлах

**ПРАВИЛЬНО:**
- Использовать только английские комментарии в `.env` файлах
- Проверять отображение файлов в разных терминалах
- Стандартизировать кодировку UTF-8 для всех текстовых файлов

---

## 🌐 Проблемы с веб-интерфейсом (дополнения)

### ❌ Проблемы с JavaScript и Bootstrap

**НЕ ДЕЛАТЬ:**
- Размещать JavaScript код в `{% block scripts %}` до загрузки Bootstrap
- Забывать передавать параметр `event` в JavaScript функции
- Использовать `bootstrap.Modal` до полной загрузки Bootstrap библиотеки

**ПРАВИЛЬНО:**
- Размещать JavaScript в `{% block extra_scripts %}` после Bootstrap
- Всегда передавать `event` параметр: `onclick="deleteVersion(123, event)"`
- Проверять доступность `bootstrap` объекта перед использованием

### ❌ Проблемы с импортами в Python

**НЕ ДЕЛАТЬ:**
- Использовать неправильные относительные импорты (`from src.config.database`)
- Импортировать из несуществующих модулей без проверки
- Смешивать абсолютные и относительные импорты в одном файле

**ПРАВИЛЬНО:**
- Использовать правильные относительные импорты (`from ....infrastructure.database.connection`)
- Проверять существование модулей перед импортом
- Следовать единому стилю импортов в проекте

---

## ⚙️ Проблемы с настройками и конфигурацией

### ❌ Проблемы с переменными окружения

**НЕ ДЕЛАТЬ:**
- Добавлять новые настройки только в `.env` без обновления `env.example` и `env.production`
- Удалять старые настройки из тестовых файлов без разрешения
- Забывать синхронизировать настройки между разными средами

**ПРАВИЛЬНО:**
- Всегда обновлять все `.env` файлы одновременно (`.env`, `env.example`, `env.production`, `env.test`)
- Сохранять существующие настройки в тестовых файлах - они могут понадобиться
- Документировать назначение каждой новой переменной

### ❌ Проблемы с обновлением документации

**НЕ ДЕЛАТЬ:**
- Реализовывать новые функции без обновления архитектурной документации
- Забывать обновлять техническое задание при изменении требований
- Оставлять устаревшую информацию в README и других документах

**ПРАВИЛЬНО:**
- Обновлять `vision.md` и `product_idea.md` при каждом значительном изменении
- Синхронизировать документацию с реальным состоянием системы
- Добавлять новые возможности в описание проекта

---

## 🔧 Проблемы с инструментами

### ❌ Проблемы с командной строкой

**НЕ ДЕЛАТЬ:**
- Использовать `&&` в PowerShell (не поддерживается)
- Создавать сложные однострочные команды с экранированием
- Забывать про различия между оболочками

**ПРАВИЛЬНО:**
- Выполнять команды по отдельности в PowerShell
- Создавать отдельные скрипты для сложной логики
- Проверять совместимость команд с целевой оболочкой

### ❌ Проблемы с подключениями

**НЕ ДЕЛАТЬ:**
- Использовать неправильные параметры подключения к БД
- Предполагать что внешние сервисы доступны
- Игнорировать ошибки подключения

**ПРАВИЛЬНО:**
- Использовать настройки из конфигурации приложения
- Проверять доступность сервисов перед операциями
- Обрабатывать все типы ошибок подключения

---

## 📋 Общие принципы "Как НЕ надо"

### 🚫 Антипринципы разработки:

1. **"Сделаю быстро, потом исправлю"** → Всегда делать правильно с первого раза
2. **"Это работает у меня"** → Тестировать в условиях, максимально близких к продакшену
3. **"Добавлю еще одну проверку"** → Понять и исправить корневую причину
4. **"Скопирую код отсюда"** → Понять логику и адаптировать под контекст
5. **"Это временное решение"** → Временные решения становятся постоянными

### 🎯 Правила предотвращения ошибок:

1. **Читать код перед изменением** - понимать контекст и зависимости
2. **Тестировать каждое изменение** - не накапливать нетестированные изменения
3. **Проверять все связанные компоненты** - изменения влияют на систему целиком
4. **Документировать предположения** - делать неявное явным
5. **Использовать типизацию** - предотвращать ошибки на этапе разработки
6. **Проверять Docker volumes** - убеждаться что файлы синхронизируются между хостом и контейнером
7. **Тестировать JavaScript в браузере** - не полагаться только на синтаксическую проверку
8. **Проверять импорты после рефакторинга** - убеждаться что все пути корректны

---

## 🔍 Чек-лист перед коммитом

- [ ] Все новые поля имеют соответствующие миграции
- [ ] Все изменения в API протестированы
- [ ] Все шаблоны проверены на синтаксические ошибки
- [ ] Все временные файлы удалены
- [ ] Все ошибки обрабатываются gracefully
- [ ] Все новые функции имеют тесты
- [ ] Документация обновлена при необходимости
- [ ] Docker volumes корректно настроены для всех конфигурационных файлов
- [ ] JavaScript код размещен в правильных блоках шаблонов
- [ ] Все импорты проверены после рефакторинга
- [ ] Кодировка файлов совместима с целевыми терминалами
- [ ] Тестовые файлы очищены из корня проекта

---

*Этот документ должен обновляться при каждой новой ошибке для непрерывного улучшения процесса разработки.*
